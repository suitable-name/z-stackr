// Not compiled: superseded by the shared `z-stackr-pipeline` crate at
// crates/stacker-pipeline/src/lib.rs (see that crate's module docs for the
// full design — memory model, apron strategy, PipelineParams/
// PipelineProgress). `apps/stacker-cli/src/lib.rs` re-exports it as
// `pub use stacker_pipeline as pipeline;`, so this directory is not
// referenced by any `mod` declaration.
